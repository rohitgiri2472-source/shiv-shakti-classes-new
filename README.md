# Shiv Shakti Classes — Full Stack Starter

frontend/ = open `index.html` with VS Code Live Server.
backend/ = run Node.js API from terminal.

The frontend calls the backend at `http://localhost:5000/api`.
MongoDB Atlas stores users, lectures, announcements and notifications.

See `backend/README.md` for complete setup.
